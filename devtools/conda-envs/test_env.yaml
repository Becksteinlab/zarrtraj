name: zarrtraj-test
channels:
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist

  # Pip-only installs
  - pip:
    - codecov
    - MDAnalysis

