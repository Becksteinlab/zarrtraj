name: zarrtraj-test
channels:
  - defaults
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist

  # MDAKit Development requires
  - MDAnalysisData
  - h5py
  - pandas

  # MDAKit requires
  - zarr

  # Pip-only installs
  - pip:
    - codecov
    - MDAnalysis

