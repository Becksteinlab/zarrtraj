name: zarrtraj-test
channels:
  - defaults
  - conda-forge
dependencies:
  ### Base depends ###
  - python>=3.9
  - pip

  ### Core requirements ###
  - MDAnalysis>=2.7.0
  - zarr>=2.11.0
  - dask

  ### AWS dependencies ###
  # AWS reading/writing
  - s3fs=2024.3.0 

  # Pip-only installs
  # - pip:


